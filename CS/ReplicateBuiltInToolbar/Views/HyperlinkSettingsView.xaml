<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:dx="http://schemas.devexpress.com/maui"
    xmlns:local="clr-namespace:HtmlEditToolbarCustomization" x:Class="HtmlEditToolbarCustomization.HyperlinkSettingsView" Padding="24">
    <ContentView.Content>
        <dx:DXStackLayout ItemSpacing="24">
            <dx:TextEdit Text="{Binding Text, Mode=TwoWay}" LabelText="{dx:HtmlEditLocalizer Id=InsertHyperlink_LinkName}"/>
            <dx:TextEdit Text="{Binding Url, Mode=TwoWay}" LabelText="{dx:HtmlEditLocalizer Id=InsertHyperlink_LinkAddress}"/>
        </dx:DXStackLayout>
    </ContentView.Content>
</ContentView>
